<ui:FluentWindow x:Class="PopClip.App.UI.AiResultWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="ClipAura AI"
                 Width="760"
                 Height="660"
                 MinWidth="620"
                 MinHeight="520"
                 WindowStartupLocation="CenterScreen"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 Background="{DynamicResource Settings.Window.Background}"
                 FontFamily="{StaticResource Settings.FontFamily}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     Title="ClipAura AI"
                     ShowMaximize="True"
                     ShowMinimize="False">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Sparkle24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Grid Grid.Row="1" Margin="28,16,28,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText"
                       Grid.Row="0"
                       Style="{StaticResource Settings.PageTitle}" />
            <TextBlock x:Name="MetaText"
                       Grid.Row="1"
                       Style="{StaticResource Settings.PageDescription}" />

            <Border x:Name="ReferencePanel"
                    Grid.Row="2"
                    Background="{DynamicResource Settings.Card.SubtleBackground}"
                    BorderBrush="{DynamicResource Settings.Stroke}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Settings.Radius.Md}"
                    Padding="12"
                    Margin="0,8,0,10">
                <Expander x:Name="ReferenceExpander"
                          Header="引用文本"
                          IsExpanded="False">
                    <ScrollViewer MaxHeight="116"
                                  VerticalScrollBarVisibility="Auto"
                                  Margin="0,8,0,0">
                        <TextBlock x:Name="ReferenceText"
                                   TextWrapping="Wrap"
                                   Foreground="{DynamicResource Settings.SubtleForeground}" />
                    </ScrollViewer>
                </Expander>
            </Border>

            <ProgressBar x:Name="LoadingBar"
                         Grid.Row="3"
                         Height="4"
                         IsIndeterminate="True"
                         Visibility="Collapsed"
                         Margin="0,0,0,10" />

            <Border Grid.Row="4"
                    Background="{DynamicResource Settings.Card.Background}"
                    BorderBrush="{DynamicResource Settings.Stroke}"
                    BorderThickness="1"
                    CornerRadius="{StaticResource Settings.Radius.Md}"
                    Padding="10">
                <ScrollViewer x:Name="MessagesScrollViewer"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="MessagesPanel" />
                </ScrollViewer>
            </Border>

            <Grid Grid.Row="5" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:TextBox x:Name="QuestionBox"
                            Grid.Column="0"
                            AcceptsReturn="True"
                            TextWrapping="Wrap"
                            VerticalScrollBarVisibility="Auto"
                            MinHeight="78"
                            MaxHeight="132"
                            Padding="12"
                            PlaceholderText="输入问题，按 Ctrl+Enter 发送"
                            KeyDown="OnQuestionKeyDown" />
                <ui:Button x:Name="SendButton"
                           Grid.Column="1"
                           Appearance="Primary"
                           Icon="{ui:SymbolIcon Send24}"
                           Content="发送"
                           Width="98"
                           Margin="10,0,0,0"
                           VerticalAlignment="Stretch"
                           Click="OnSendClicked" />
            </Grid>
        </Grid>

        <Border Grid.Row="2"
                Background="{DynamicResource Settings.Card.Background}"
                BorderBrush="{DynamicResource Settings.Stroke}"
                BorderThickness="0,1,0,0"
                Padding="28,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Appearance="Secondary"
                           Content="关闭"
                           Width="92"
                           Margin="0,0,10,0"
                           Click="OnCloseClicked" />
                <ui:Button Appearance="Secondary"
                           x:Name="CopyButton"
                           Icon="{ui:SymbolIcon Copy24}"
                           Content="复制最新回复"
                           Width="132"
                           Margin="0,0,10,0"
                           Click="OnCopyClicked" />
                <ui:Button Appearance="Primary"
                           x:Name="ReplaceButton"
                           Icon="{ui:SymbolIcon Edit24}"
                           Content="替换选中文本"
                           Width="142"
                           Click="OnReplaceClicked" />
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
