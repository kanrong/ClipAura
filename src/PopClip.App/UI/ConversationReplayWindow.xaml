<ui:FluentWindow x:Class="PopClip.App.UI.ConversationReplayWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="ClipAura 对话回放"
                 Width="720"
                 Height="640"
                 MinWidth="520"
                 MinHeight="380"
                 WindowStartupLocation="CenterScreen"
                 WindowBackdropType="Mica"
                 ExtendsContentIntoTitleBar="True"
                 Background="{DynamicResource Settings.Window.Background}"
                 FontFamily="{StaticResource Settings.FontFamily}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0"
                     x:Name="TitleBar"
                     Title="对话回放"
                     Style="{StaticResource Settings.CompactTitleBar}"
                     ShowMaximize="True"
                     ShowMinimize="True">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="History24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Border Grid.Row="1"
                Margin="14,8,14,4"
                Background="{DynamicResource Settings.Card.SubtleBackground}"
                BorderBrush="{DynamicResource Settings.Stroke}"
                BorderThickness="1"
                CornerRadius="{StaticResource Settings.Radius.Sm}"
                Padding="10,8">
            <StackPanel>
                <TextBlock x:Name="MetaText"
                           FontSize="11.5"
                           Foreground="{DynamicResource Settings.SubtleForeground}" />
                <TextBlock x:Name="ReferenceText"
                           FontSize="12"
                           Margin="0,4,0,0"
                           Foreground="{DynamicResource Settings.Muted}"
                           TextWrapping="Wrap"
                           MaxHeight="80"
                           TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2"
                      Padding="14,4,14,14"
                      VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="MessagesPanel" />
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
